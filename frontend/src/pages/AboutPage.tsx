import { AboutContent } from '../components/marketing/AboutContent';

/** The same content shown publicly at /about, reached from inside the console. */
export default function AboutPage() {
  return <AboutContent />;
}
